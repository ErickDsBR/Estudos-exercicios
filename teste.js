const nomelista = document.querySelector().value;
    let a = 'pamonha'; // variavel de teste
    const lista = [
        
    ];
    lista.push({nome: nomelista });
    const nomes = lista.map(list => list.nome);
    localStorage.setItem(nomes);
    