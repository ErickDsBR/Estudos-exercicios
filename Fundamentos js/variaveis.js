// variaveis var let const

//para começar podemos dar nome as variaveis para guardarmos o valor ex: var nome, let idade, const local e logo apos usamos = para atribuir um valor 

// para iniciar uma variavel usamos var
// X e um espaço na memoria aonde 10 foi guardado valor, o console.log chama esse espaço e mostra seu valor no terminal.
   var x = 10;
   console.log(x)

//O var pode ter seu valor reatribuido 
   var X = 10;
   x = 15;
   console.log(x);
// sobre escrevemos seu valor desta forma isto tambem serve para o let 
   let x = 10;
   x = 20;
   console.log(x);
//O const ja e diferente pois se trata de um valor constante no caso não pode ser alterado seu valor uma vez atribuido não pode ser reatribuido
   const x = 10;
   x = 20;
   console.log(x)
//Se rodar este comando dara erro! apague a linha 20 e codigo retornara 10 no terminal normalmente ! 